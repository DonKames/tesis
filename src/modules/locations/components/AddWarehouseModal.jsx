import React from 'react';
import { Button, Modal } from 'react-bootstrap';

export const AddWarehouseModal = () => {
    return (
        <>
            <Button></Button>
            <Modal></Modal>
        </>
    );
};
